{{>/layout/header}}

<div class="container mt-5">
    <h2 style="text-align:center">경기장 목록</h2>
    <br>

    <table class="table table-hover">
        <thead class="thead-dark">
            <tr>
                <th scope="col">번호</th>
                <th scope="col">경기장 이름</th>
                <th scope="col">개장일</th>
                <th scope="col">수정</th>
                <th scope="col">삭제</th>
            </tr>
        </thead>
        <tbody>
            {{#stadiums}}
            <tr>
                <th id="my-stadium-id" scope="row">{{id}}</th>
                <td>{{name}}</td>
                <td>{{formatCreateDate}}</td>
                <td><i class="fa-solid fa-wrench"></i></td>
                <td id="my-stadium-delete"><i class="fa-solid fa-trash"></i></td>
            </tr>
            {{/stadiums}}
        </tbody>
    </table>

</div>

<script>
    $("#my-stadium-delete").click(() => {
        deleteStadium();
    });

    async function deleteStadium() {

    }
</script>

</body>

</html>

{{>/layout/footer}}